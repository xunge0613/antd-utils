---
name: Paste Tags
menu: Demos
---

import { Playground, Props } from 'docz'
import { PasteTags } from './paste-tags'

# Paste Tags


<Playground>
  <PasteTags />
</Playground>

``` javascript
// Try input 'a,b,c,1,2,3,a'

export const PasteTags: FC = () => {
  const [form] = useForm();
  return (
    <>
      <Form form={form}>
        <Form.Item name="name">
          <Select
            mode="tags"
            onChange={(value: string[]) => pasteTags(value, form, "name")} />
        </Form.Item>
      </Form>
    </>
  )
}
```